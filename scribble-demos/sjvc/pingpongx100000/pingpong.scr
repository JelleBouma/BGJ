module pingpong.Pingpong;

data <java> "java.lang.Integer" from "rt.jar" as Int;

global protocol Pingpong(role C, role S) {
    choice at C {
        Ping() from C to S;
        Pong() from S to C;
        do Pingpong(C, S);
    }
    or {
        Bye() from C to S;
    }
}